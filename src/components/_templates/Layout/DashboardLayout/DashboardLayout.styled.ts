import { createStyles } from "@/utils/styles";

export const contentContainerStyle = createStyles({
  height: "100svh",
  backgroundColor: "white",
  padding: "0 36px",
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
});

export const mainContentContainerStyle = createStyles({
  flexDirection: "column",
  alignItems: "center",
  maxWidth: "650px",
  width: "100%",
});
