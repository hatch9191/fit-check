import { createStyles } from "@/utils/styles";

export const menuIconStyle = createStyles({
  fontSize: 24,
});

export const containerStyle = createStyles({
  paddingTop: 10,
});

export const menuStyle = createStyles({
  boxShadow: "none",
});
